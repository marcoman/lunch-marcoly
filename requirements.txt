# Python dependencies for all lunch-marcoly examples (Python 3.12+).
# Use pyenv to install Python — see README.md and .python-version.
# Install into the repository virtual environment:
#   pyenv install 3.12
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
#
# 00-reference-code and 01-reference-agent stub/ollama modes use the stdlib only.
# boto3 is required for AGENT_LLM_MODE=bedrock (01-reference-agent).
# launchdarkly-server-sdk supports LaunchDarkly examples in other directories.
# launchdarkly-server-sdk-ai is required for AgentControl (20-agent-config/…).
boto3>=1.35
launchdarkly-server-sdk>=9.0
launchdarkly-server-sdk-ai>=0.7
typing_extensions>=4.0
