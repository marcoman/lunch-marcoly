# Node.js (console)

Console grid navigator for [11-create-eval-flag](../application.md).

## Run

```bash
npm install ..
npm install
npm start
```

Re-evaluates the flag every 500 ms while the grid is open.

```bash
node 11-create-eval-flag.js --evaluate-once alice
```
