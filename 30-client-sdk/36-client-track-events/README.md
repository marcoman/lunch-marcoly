# 36-client-track-events

**Status: stub — not implemented yet.**

LaunchDarkly **client-side `track()`**: custom events from the browser grid.

Event keys (lab):

- `login_completed`
- `grid_move`

Not Twilio Segment `track()` ([34](../34-synced-segments-twilio/)). Not server
custom metrics ([16-adaptive-triggers](../../99-use-cases/16-adaptive-triggers/)).

Keywords: **track** · **custom events** · **client-side SDK** · **metrics**

Docs: [Sending custom events](https://launchdarkly.com/docs/sdk/features/events)

## Intended aha

Log in and move. LaunchDarkly **Live events** (or a metric attached to these
keys) shows `login_completed` and `grid_move` for that context.

## Implementation

| Language | Directory | Status |
|----------|-----------|--------|
| *(none yet)* | — | Stub |
